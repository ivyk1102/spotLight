const btn = btn


//1=no 2=yes
//import api data?
//const hashset = inserting names of artists that we've seen
//const array = array of api artists. array size = 10
//while (i<10)
//if (!set.contains(artist_name))
//array[i]=artist_name
//set.add(artist-name)
//i++
// Detect clicks on the button

if (btn) {
  btn.onclick = function() {
    console.log("clicked")
    // The JS works in conjunction with the 'dipped' code in style.css
    btn.classList.toggle("dipped");
    //IDK WHAT THIS DOES T_T
  }
}
else if (btn2) {
  btn2.onclick = function() {
    console.log("clicked")
    // The JS works in conjunction with the 'dipped' code in style.css
    btn2.classList.toggle("dipped");
    //IDK WHAT THIS DOES T_T
  }
}


// This is a single line JS comment
/*
This is a comment that can span multiple lines 
- use comments to make your own notes!
*/console.log(btn)

//1=no 2=yes
//import api data?
//const hashset = inserting names of artists that we've seen
//const array = array of api artists. array size = 10
//while (i<10)
//if (!set.contains(artist_name))
//array[i]=artist_name
//set.add(artist-name)
//i++
// Detect clicks on the button

if (btn) {
  btn.onclick = function() {
    console.log("clicked")
    // The JS works in conjunction with the 'dipped' code in style.css
    btn.classList.toggle("dipped");
    //IDK WHAT THIS DOES T_T
  }
}

else if (btn2) {
  btn2.onclick = function() {
    console.log("clicked")
    // The JS works in conjunction with the 'dipped' code in style.css
    btn2.classList.toggle("dipped");
    //IDK WHAT THIS DOES T_T
  }
}


// This is a single line JS comment
/*
This is a comment that can span multiple lines 
- use comments to make your own notes!
*/
